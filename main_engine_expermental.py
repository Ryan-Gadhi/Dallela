from interpeter.engine import Engine

eng = Engine()


# print( eng.compute("what is the most active rig company") )
# print( eng.compute("show me the most producing fields today") )
# print(eng.compute("many active wellbore from baker "))
#
# print(eng.compute("efficiency") )
#
# print( eng.compute("Show me the fields with the most producing wells") )
# print( eng.compute("How many active rigs 2 weeks ago") )
#
# print( eng.compute("what is the efficiency for baker hughes") )
#
# print( eng.compute("What is the weather for 123 in tokyo") )

