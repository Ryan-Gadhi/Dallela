from interpeter.engine import Engine

e = Engine()
print(e.compute("what is the numebr of active rigs todey"))
# print(e.compute("what is the product line "))

#  print(e.compute('what is the product line in dammam wells'))
# print(e.compute('what is the number of active rigs'))
# what baker hughes product line is currently active in dammam